{
  "name": "superscroll",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude Code skill: one sentence in, a scroll-scrubbed video site out.",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test tests/**/*.test.mjs"
  },
  "devDependencies": {
    "playwright": "1.55.0"
  }
}
